# Example secrets (TEMPLATE ONLY)
# Replace values and apply via your secret manager (recommended).

---
apiVersion: v1
kind: Secret
metadata:
  name: milvus-minio-credentials
  namespace: healthtourism
type: Opaque
stringData:
  accessKey: "CHANGE_ME"
  secretKey: "CHANGE_ME"

