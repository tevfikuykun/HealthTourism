apiVersion: v1
kind: Secret
metadata:
  name: health-tourism-secrets
  namespace: health-tourism
type: Opaque
stringData:
  # Database Credentials (Change in production!)
  mysql-root-password: "root"
  mysql-username: "root"
  mysql-password: "root"
  
  # JWT Secret (Change in production!)
  jwt-secret: "your-super-secret-jwt-key-change-in-production"
  
  # IPFS Encryption Key (Change in production!)
  ipfs-encryption-key: "your-super-secret-ipfs-key-change-in-production"
  
  # External API Keys (Add your keys)
  google-speech-api-key: ""
  azure-speech-api-key: ""
  azure-speech-region: ""
