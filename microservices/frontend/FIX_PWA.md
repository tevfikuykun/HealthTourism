# PWA Plugin Kurulumu (Opsiyonel)

Frontend şu anda PWA plugin olmadan çalışıyor. PWA özelliklerini etkinleştirmek için:

```bash
cd microservices/frontend
npm install vite-plugin-pwa --save-dev
```

Sonra `vite.config.js` dosyasında yorum satırlarını kaldırın.

