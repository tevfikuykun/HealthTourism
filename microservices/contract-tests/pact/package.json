{
  "name": "health-tourism-contract-tests",
  "version": "1.0.0",
  "description": "Contract tests for Health Tourism microservices",
  "scripts": {
    "test": "jest",
    "pact:publish": "pact-broker publish pacts --consumer-app-version=$npm_package_version"
  },
  "dependencies": {
    "@pact-foundation/pact": "^13.0.0",
    "jest": "^29.0.0"
  },
  "devDependencies": {
    "@pact-foundation/pact-node": "^13.0.0"
  }
}
