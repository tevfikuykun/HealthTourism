package com.healthtourism.jpa.audit;

import org.hibernate.envers.query.AuditEntity;

/**
 * Audit Entity Helper
 * Re-export Hibernate Envers AuditEntity for convenience
 */
public class AuditEntityHelper {
    // Use Hibernate Envers AuditEntity directly
    // Example: AuditEntity.id().eq(patientId)
}

