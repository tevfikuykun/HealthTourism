name: CI/CD Pipeline

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master, develop ]

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    
    strategy:
      fail-fast: false
      matrix:
        service:
          - auth-service
          - user-service
          - hospital-service
          - payment-service
          - reservation-service
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up JDK 25
      uses: actions/setup-java@v4
      with:
        java-version: '25'
        distribution: 'temurin'
        cache: maven
    
    - name: Run tests
      working-directory: microservices/${{ matrix.service }}
      run: mvn -B clean test

    - name: Upload coverage report (if generated)
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: jacoco-${{ matrix.service }}
        path: microservices/${{ matrix.service }}/target/site/jacoco/
        if-no-files-found: ignore
    
    - name: Generate test report
      if: always()
      uses: dorny/test-reporter@v1
      with:
        name: Test Results (${{ matrix.service }})
        path: microservices/${{ matrix.service }}/target/surefire-reports/*.xml
        reporter: java-junit
        fail-on-error: false

  build:
    needs: test
    runs-on: ubuntu-latest
    timeout-minutes: 20
    
    strategy:
      fail-fast: false
      matrix:
        service:
          - auth-service
          - user-service
          - hospital-service
          - payment-service
          - reservation-service
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up JDK 25
      uses: actions/setup-java@v4
      with:
        java-version: '25'
        distribution: 'temurin'
        cache: maven
    
    - name: Build Docker image
      working-directory: microservices/${{ matrix.service }}
      run: |
        docker build -t healthtourism/${{ matrix.service }}:latest .
    
    - name: Build application
      working-directory: microservices/${{ matrix.service }}
      run: mvn -B clean package -DskipTests

  security-scan:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
    - uses: actions/checkout@v4
    
    - name: Run Trivy vulnerability scanner
      uses: aquasecurity/trivy-action@master
      with:
        scan-type: 'fs'
        scan-ref: '.'
        format: 'sarif'
        output: 'trivy-results.sarif'
    
    - name: Upload Trivy results to GitHub Security
      uses: github/codeql-action/upload-sarif@v2
      with:
        sarif_file: 'trivy-results.sarif'

